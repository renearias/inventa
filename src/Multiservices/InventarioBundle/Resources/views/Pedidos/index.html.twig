{% extends app.request.isXmlHttpRequest ? '::baseajax.html.twig' : '::basesmartpanel.html.twig' %}
{% block content -%}
    <h1>Entregas Realizadas</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Codigo Entrega</th>
                
                <th>Fecha</th>
                <th>Cliente</th>
                <th>Estado</th>
                
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="#{{ path('pedidos_show', { 'id': entity.codpedido }) }}">{{ entity.codpedido }}</a></td>
                <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i') }}{% endif %}</td>
                <td>{{ entity.codcliente }}</td>
                <td>{{ entity.estado }}</td>
                
                <td>
                <ul>
                    <li>
                        <a href="#{{ path('pedidos_show', { 'id': entity.codpedido }) }}">mostrar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="#{{ path('pedidos_new') }}">
                Crear nueva entrada
            </a>
        </li>
    </ul>
                <script type="text/javascript">

	pageSetUp();

                            
	/*
	 * ALL PAGE RELATED SCRIPTS CAN GO BELOW HERE
	 * eg alert("my home function");
	 * 
	 * var pagefunction = function() {
	 *   ...
	 * }
	 * loadScript("js/plugin/_PLUGIN_NAME_.js", pagefunction);
	 * 
	 */
	
	// PAGE RELATED SCRIPTS
	
	// pagefunction	
	var pagefunction = function() {
		//console.log("cleared");
		
	  
	    

	};
       $(function() {  
        loadScript("{{asset('js/arxis-postforms.js')}}",pagefunction);
        });
</script> 
    {% endblock %}
 