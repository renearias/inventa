{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ArticulosDetalle</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Estatus</th>
                <td>{{ entity.estatus }}</td>
            </tr>
            <tr>
                <th>Atributos</th>
                <td>{{ entity.atributos }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('articulosdetalle') }}">
            Regresar a la lista
        </a>
    </li>
    <li>
        <a href="{{ path('articulosdetalle_edit', { 'id': entity.id }) }}">
            Editar
        </a>
    </li>
    <li>{{ form_start(delete_form) }}{{ form_row(delete_form.submit, { 'label': 'Eliminar'}) }}{{ form_end(delete_form) }}</li>
</ul>
{% endblock %}
